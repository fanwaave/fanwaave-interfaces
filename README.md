# fanwaave-interfaces

Data-only contracts for Fan and push-notification delivery across web, mobile, and desktop. Schema: `schema/v1`. Generated TypeScript and Dart live under `generated/` and must stay types-only.
